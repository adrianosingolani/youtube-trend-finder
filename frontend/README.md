# Frontend (Vite + React)

Full project setup (with the backend and database) lives in the **[root README](../README.md)**.

```bash
npm install
npm run dev
```

Dev server default: http://localhost:5173

Regenerate API types after the backend serves OpenAPI (see root README):

```bash
npm run openapi:types
```
